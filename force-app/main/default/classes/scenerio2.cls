public class scenerio2
{
   public static void updatechildFieldrec(list<user> ulst)
   {
       list<Opportunity> opplst = new list <opportunity>();
       
       for(user u : ulst)
       {
           opportunity opp = new opportunity();
           
       }
   }
}